name: Docker Image CI

on:
  push:
    branches: [ "master" ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Hadolint
        uses: hadolint/hadolint-action@v2

      - name: Run Hadolint lab 2
        run: hadolint ./lab2/Dockerfile
